export enum Events {
  show_details = 'Show Details',
  valid_responses = 'Valid Responses',
  response_target = 'Response Target'
}

export enum EventsDescription {
  show_details = 'Displays detailed information about the event.',
  valid_responses = 'Tracks the number of valid responses collected.',
  response_target = 'Indicates the target number of responses to be achieved.'
}
